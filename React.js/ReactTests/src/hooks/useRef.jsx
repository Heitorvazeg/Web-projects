/* const labelRef = useRef(''); Precisa colocar o ref={meuRef} no componente.
Usa de Referências entre valores
Manipula o DOM do valor através da referência, sem re-renderização.
Acessa o valor com o .current, pq é um objeto.
 Como uma caixinha que não dispara re-render. */